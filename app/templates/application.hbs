<nav class="navbar navbar-inverse">
  <div class="container-fluid">
    <div class="navbar-header">
      <a class="navbar-brand" href="#">BudgetApp</a>
    </div>
    <ul class="nav navbar-nav">
      <li ><a href="#">{{#link-to 'application' tagName="li" }}Home{{/link-to}}</a></li>
      <!-- implement home page (using link-to)-->
      <li><a href="#">Information</a></li>
      <!-- implement information page (using link-to)-->
      <li><a href="#">Chart</a></li>
      <!-- implement chart page (using link-to)-->
    </ul>
    <ul class="nav navbar-nav navbar-right">
      {{#if  session.isAuthenticated}}
        <div>
          <li><a>Loging in as {{session.currentUser.email}}</a></li>
          <li><a href="#" {{action 'signOut'}}><span class="glyphicon glyphicon-log-in"></span> Log Out</a></li>
        </div>
      {{else}}
          <li>
          <ul>
            <li> {{input type="email" id="userEmail" value=emailAddressC class="form-control" placeholder="e-mail address." autofocus="autofocus"}}</li>
            <li> {{input type="password" id="userPassword"value=textC class="form-control" placeholder="password"}} </li>
          </ul>
          </li>

          <li>
            <ul>
            <li><button class="btn" {{action 'signIn'}}><span class="glyphicon glyphicon-log-in" ></span> Login</button></li>
            {{#link-to 'signup' tagName="li" }}<button class="btn"><span class="glyphicon glyphicon-log-in" ></span> Sign Up</button>{{/link-to}}
          </ul>
          </li>

      {{/if}}
    </ul>
  </div>
</nav>


{{outlet}}
